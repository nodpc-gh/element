# Element IDE v2.0

**Веб-IDE для визуального программирования с системой портов и соединений**

![Version](https://img.shields.io/badge/version-2.0.0-blue.svg)
![Platform](https://img.shields.io/badge/platform-Windows%20|%20Linux-lightgrey.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

## 🛠️ Технологии

### Написано на **FASM** (Flat Assembler) + **JavaScript**

**Сервер на ассемблере, интерфейс на чистом JS!**

| Компонент | Технология |
|-----------|------------|
| **Веб-сервер** | FASM x86 Assembly (модульная архитектура) |
| **Сокеты (Win32)** | Win32 API (ws2_32.dll) |
| **Сокеты (Linux)** | Linux syscalls (int 0x80) |
| **HTTP** | Ручная реализация |
| **API** | REST (`/api/elements`, `/api/projects`, `/api/projects/{id}`) |
| **База данных** | Файловая (element.db, DBM-формат) |
| **Frontend** | HTML5/CSS3/JavaScript (чистый JS, без библиотек) |
| **Подсветка кода** | Кастомная (FASM-синтаксис) |

### Почему FASM?

- ⚡ **Скорость** — нативный машинный код
- 📦 **Размер** — dev ~254 КБ, public ~246 КБ, linux ~248 КБ
- 🎯 **Контроль** — полная работа с памятью
- 🏛️ **Классика** — дань уважения низкоуровневому программированию
- 🐧 **Linux** — нативный ELF без библиотек (syscalls int 0x80)

### Статистика кода

```
src_v2/            ~1300 строк  ассемблера (10 модулей, dev + auth)
gitverse/src/      ~1200 строк  ассемблера (9 модулей, public, без auth)
linux/src/         ~1200 строк  ассемблера (8 модулей, ELF + syscalls)
output/www/        ~2000 строк  JavaScript (8 модулей)
output/www/         ~400 строк  CSS
──────────────────────────────────────
Итого:             ~4900 строк  кода
```

### Сборка

```batch
build              # dev-версия (src_v2/ → output/server.exe)
build_pub          # public-версия (gitverse/src/ → output/server_public.exe)
```

**Linux:**
```bash
cd linux && make   # ELF-исполняемый файл (element_linux)
```

**Результат:**
- Три бинарника: dev (с авторизацией), public (без auth), linux (ELF)
- Кроссплатформенность: Windows + Linux
- Самодостаточный веб-сервер с REST API
- Не требует библиотек / установки

---

## 🎯 Вдохновение

Проект вдохновлён легендарной средой визуального программирования **HiAsm** (High Assembler).

### Что взято от HiAsm:

| Концепция | Реализация в Element IDE |
|-----------|-------------------------|
| **Элементы-ноды** | UI компоненты с портами |
| **Порты** | 4 стороны (in, out, id, data) |
| **Соединения** | Цветовая индикация типов |
| **Потоки данных** | От выхода к входу |
| **События** | Event-ориентированные связи |
| **Идентификация** | ID портов для точных связей |

### Философия

> *"Программирование должно быть визуальным. Поток данных виден. События осязаемы."*

**Element IDE** продолжает идеи HiAsm:
- 🧩 **Визуальная сборка** — как конструктор
- 🔄 **Потоки** — данные текут по линиям
- ⚡ **События** — реакции на действия
- 📊 **Данные** — передача значений
- 🔍 **Наглядность** — всё на виду

### Отличия от HiAsm

| HiAsm | Element IDE |
|-------|-------------|
| Windows приложение | Веб-интерфейс |
| Сложный интерфейс | Минимализм |
| Много элементов | Базовый набор |
| Бинарный формат | JSON экспорт |
| Закрытый код | Открытый (MIT) |

### Памяти HiAsm

Этот проект — дань уважения создателям HiAsm, которые показали миру, что программирование может быть:
- **Визуальным**
- **Интуитивным**
- **Красивым**

---

## 📖 Описание

Element IDE — это легковесная веб-IDE для визуального проектирования потоков данных и событий. Сервер написан на **FASM** (чистый ассемблер), интерфейс — на чистом **JavaScript** без фреймворков.

**Особенности:**
- 🎨 Визуальный редактор с сеткой (Drag&Drop)
- 📦 Палитра UI элементов (Button, Label, Edit, CheckBox, Panel)
- 🔴 Порты на 4 сторонах элемента (in, out, id, data)
- 🔗 Соединения между портами с цветовой индикацией
- 📋 Копирование/вставка элементов
- ⚙️ Компилятор в JSON с буфером обмена и скачиванием
- 🖱️ Контекстные меню для управления
- 🐚 **Активная консоль** — все действия логируются в формате `$ cmd args`
- 🤖 **Элемент OpenCode** — AI-агент с терминальным стилем
- 📝 **Редактор кода** с подсветкой FASM-синтаксиса
- 🗄️ **База данных** элементов (просмотр/поиск/фильтрация)
- 💾 **Сохранение/загрузка** проектов через REST API
- 🔌 **Панель портов** с описанием назначения каждого порта

## 🚀 Быстрый старт

### Установка

1. Скачайте репозиторий:
```bash
git clone https://gitverse.ru/nodpc/element.git
cd element
```

2. Соберите сервер (требуется [FASM](https://flatassembler.net/)):
```batch
build
```

3. Или запустите готовый бинарник:
```batch
output\element.exe
```

Сервер запустится на порту **8080** и автоматически откроет браузер:
```
http://localhost:8080
```

## 📖 Использование

### Создание элементов

1. Перетащите элемент из левой панели на рабочую область
2. Перемещайте элемент перетаскиванием
3. Двойной клик → удалить элемент

### Управление портами

**Правый клик на элементе:**
- **+ (зелёный)** — добавить порт на сторону
- **− (красный)** — удалить порт со стороны
- **📋 Копировать** — создать копию элемента со смещением
- **🗑️ Удалить** — удалить элемент

### Соединения

1. Кликните на любом порту (кружок на границе элемента)
2. Потяните к другому порту (пунктирная линия следует за мышью)
3. Кликните на целевом порту → соединение создано

**Типы соединений (цвета):**
- 🟢 **Зелёный** (data) — путь данных
- 🟣 **Фиолетовый** (event) — события
- 🟠 **Оранжевый пунктир** (id) — идентификация

**Правило:** В один порт — только одно соединение!

### Контекстные меню

| Объект | Меню |
|--------|------|
| **Элемент** | Управление портами (+/−), Копировать, Удалить |
| **Порт** | Удалить порт |
| **Линия** | Удалить соединение, Показать тип |

### Консоль

Все действия с элементами отображаются в нижней панели **Консоль** в формате команды:

```
[14:32:05] $ mkEl btn el_5 at (100, 200)
[14:32:07] $ conn port_a -> port_b (event)
[14:32:10] $ delEl el_5 removed
[14:32:15] $ compile 3 elements, 2 connections
```

### Компилятор (JSON экспорт)

Нажмите **⚙️** в toolbar для экспорта проекта:

```json
{
  "version": "2.0",
  "elements": [...],
  "connections": [...]
}
```

**Кнопки экспорта:**
- **📋 Копировать JSON** — в буфер обмена
- **💾 Скачать файл** — сохранить `element_project.json`

### Сохранение проекта

Проекты сохраняются на сервер через REST API:
- **💾 Save** — сохранение текущего проекта
- **📂 Загрузить** — выбор и загрузка проекта из списка

### Панель свойств

Правая панель отображает свойства выделенного элемента:
- **Элемент** — имя, позиция, размер
- **Данные** — тип, ID
- **Статистика** — количество событий
- **Код** — редактор кода элемента (с подсветкой FASM)
- **🔌 Порты** — список портов с описанием и статусом соединения

## 🏗️ Структура проекта

```
Element/
├── src_v2/                      # Исходники dev-версии (с auth)
│   ├── main_server.asm          # Точка входа
│   ├── code.inc                 # HTTP-сервер + API хендлеры
│   ├── data.inc                 # Данные (MIME, буферы, заголовки)
│   ├── db.inc                   # Структуры БД (DBHeader, DBIndex, DBRecord)
│   ├── db_core.inc              # Ядро БД: create, read, delete, find
│   ├── db_users.inc             # Пользователи (element.db)
│   ├── db_elements.inc          # Элементы палитры (GET /api/elements)
│   ├── db_projects.inc          # Проекты (префикс project:)
│   ├── auth.inc                 # Сессии, login/register/logout
│   └── imports.inc              # Импорты Win32 API
├── gitverse/src/                # Исходники public-версии (без auth)
│   └── ...                      # Те же модули, кроме auth.inc
├── linux/                       # Linux-порт (ELF + syscalls)
│   ├── src/
│   │   ├── main_server.asm      # Точка входа (ELF)
│   │   ├── syscall.inc          # Макросы Linux syscalls (int 0x80)
│   │   ├── code.inc             # HTTP-сервер + API хендлеры
│   │   ├── data.inc             # Данные (MIME, буферы, заголовки)
│   │   ├── db.inc               # Структуры БД + буферы
│   │   ├── db_core.inc          # Ядро БД
│   │   ├── db_elements.inc      # Элементы палитры
│   │   └── db_projects.inc      # Проекты
│   ├── www/                     # Фронтенд (копия)
│   ├── Makefile                 # Сборка (fasm)
│   └── element_linux            # Готовый ELF-бинарник
├── output/
│   ├── server.exe               # Dev-сборка (с auth)
│   ├── server_public.exe        # Public-сборка (без auth)
│   └── www/                     # Статические файлы (frontend)
│       ├── index.html           # Главная страница
│       ├── style.css            # Стили (~1900 строк)
│       ├── app.js               # Основная логика IDE
│       ├── console.js           # Активная консоль
│       ├── compiler.js          # JSON-компилятор
│       ├── database.js          # База данных элементов
│       ├── element_templates.js # Шаблоны элементов
│       ├── elements.js          # Палитра элементов
│       ├── fasm_highlight.js    # Подсветка FASM
│       └── source_editor.js     # Редактор кода
├── build.bat                    # Сборка dev (src_v2/)
├── build_pub.bat                # Сборка public (gitverse/src/)
├── run.bat                      # Запуск dev (output\server.exe)
├── run_pub.bat                  # Запуск public (output\server_public.exe)
├── stop.bat                     # Остановка сервера
├── status.bat                   # Статус сервера
├── API.md                       # REST API документация
└── README.md                    # Документация (этот файл)
```

## 🔧 Технические детали

### Сервер (FASM)

- **FASM** — Flat Assembler
- **Win32 API** — сокеты (ws2_32.dll), файловый ввод-вывод (kernel32.dll)
- **Linux** — нативные syscalls (int 0x80), ELF executable
- **Порт** — 8080
- **accept-цикл** — однопоточная последовательная обработка
- **API** — `GET /api/elements`, `GET /api/projects`, `GET /api/projects/{id}`, `POST /api/prj/{name}`
- **База данных** — единый файл `element.db` с префиксами записей (`user:`, `project:`, `element:`)
- **Три версии**: `src_v2/` — с авторизацией (dev), `gitverse/src/` — публичная без auth, `linux/src/` — ELF под Linux

### Frontend (JS)

- Чистый JavaScript ES5 — без фреймворков и библиотек
- **Модули:** компилятор, консоль, редактор кода, база данных
- **Консоль:** cmd() — формат командной оболочки с timestamp
- **Подсветка:** кастомный парсер FASM-синтаксиса (комментарии, строки, ключевые слова)
- **Совместимость** — Chrome/Edge/Firefox

### Сборка

```batch
build           # dev (src_v2/ → output/server.exe)
build_pub       # public (gitverse/src/ → output/server_public.exe)
```

**Linux:**
```bash
cd linux && make   # ELF-бинарник element_linux
```

### Команды

```batch
# Запуск dev
run

# Запуск public
run_pub

# Проверка
status

# Остановка
stop
```

**Linux:**
```bash
cd linux && make && ./element_linux   # собрать и запустить
```

## 📋 Функционал

| Функция | Статус |
|---------|--------|
| Перетаскивание элементов | ✅ |
| Перемещение по полю | ✅ |
| Порты (4 стороны) | ✅ |
| Добавление/удаление портов | ✅ |
| Соединения между портами | ✅ |
| Пунктирная линия предпросмотра | ✅ |
| Цветовая индикация типов | ✅ |
| Правило: 1 порт = 1 соединение | ✅ |
| Контекстное меню элемента | ✅ |
| Контекстное меню соединения | ✅ |
| Контекстное меню порта | ✅ |
| Копирование элементов | ✅ |
| Дублирование элементов (Ctrl+D) | ✅ |
| Компилятор в JSON | ✅ |
| Экспорт в файл / буфер обмена | ✅ |
| Сохранение/загрузка проекта (REST API) | ✅ |
| Панель свойств элемента | ✅ |
| Панель портов с описанием | ✅ |
| Активная консоль ($ cmd args) | ✅ |
| Редактор кода с подсветкой FASM | ✅ |
| База данных элементов (просмотр/поиск) | ✅ |
| Элемент OpenCode (AI-агент) | ✅ |
| Автооткрытие браузера | ✅ |
| REST API: список проектов, загрузка по ID, сохранение | ✅ |
| REST API: палитра элементов (GET /api/elements) | ✅ |
| Загрузка проекта с сервера из списка | ✅ |
| Две версии сборки: dev (auth) и public | ✅ |
| Корректная маршрутизация API | ✅ |
| Статистика событий | ✅ |

## 🎯 Планы развития

- [ ] Undo/Redo (история действий)
- [ ] Группировка элементов
- [ ] Масштабирование рабочей области
- [ ] Поиск по элементам
- [ ] Экспорт в ASM/JS
- [ ] Темы оформления
- [ ] Плагины / кастомные элементы через API
- [ ] WebSocket для real-time коллаборации
- [x] Портирование на Linux (ELF, syscalls)

## 📸 Скриншоты

### Рабочая область
![Workspace](screenshots/workspace.png)

### Контекстное меню
![Context Menu](screenshots/context-menu.png)

### Компилятор JSON
![Compiler](screenshots/compiler.png)

## 🤝 Вклад

1. Fork репозиторий
2. Создай ветку (`git checkout -b feature/amazing-feature`)
3. Commit изменений (`git commit -m 'Add amazing feature'`)
4. Push в ветку (`git push origin feature/amazing-feature`)
5. Открой Pull Request

## 📄 Лицензия

MIT License — см. файл [LICENSE](LICENSE) для деталей.

## 👤 Автор

**Element IDE Team**

## 🙏 Благодарности

- FASM — Flat Assembler
- Win32 API — Microsoft
- Linux kernel — Linus Torvalds
- HTML5/CSS3/JS — W3C

---

**Version:** 2.0.0  
**Last Updated:** 2026-06-12  
**Status:** Stable Release
